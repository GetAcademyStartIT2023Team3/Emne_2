// entrypoint
//
function main() {
    showModel('Kurs', model.kurs);
    showModel('Emner', model.emner);
    showModel('tema', model.tema);
    showModel('artikler', model.artikler);
    showModel('atomer', model.atomer);
    updateView();
}
